export const GAME_NAME = "Shattered Dominion";

/** Incrementar quando o protocolo cliente⇄servidor mudar de forma incompatível. */
export const PROTOCOL_VERSION = 1;

export const TICKS_PER_SECOND = 10;

/** 1 unidade de tempo do jogo (≈1 turno de roguelike) = 10 ticks = 1s. */
export const TICKS_PER_TIME_UNIT = 10;

export const MAX_PLAYERS = 8;
