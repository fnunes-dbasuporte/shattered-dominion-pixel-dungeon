# ── Servidor ─────────────────────────────────────────────────────────
PORT=2567
# Em produção o bind DEVE ser 127.0.0.1 (atrás do nginx).
HOST=localhost

# ── Cliente (apenas build de produção) ───────────────────────────────
# No dev deixe comentado: o Vite faz proxy de /colyseus para o servidor.
# VITE_WS_ENDPOINT=wss://www.pixelforgegames.com.br/shattered-dominion-ws
