# Dependências e build
node_modules/
dist/
dist-prod/
*.tsbuildinfo
coverage/
.vite/
tools/viz/out*.html
tools/site/out/

# Segredos
.env
.env.*
!.env.example
# URL pública do WS de produção — sem segredos, necessária ao build
!packages/client/.env.production

# Infra da VM de produção — NUNCA commitar (repo é público; contém IP, portas e detalhes de acesso)
INFRA-VM-*.md
docs/infra/INFRA-VM-*.md
.infra/

# Documentos de planejamento, orientação e testes manuais — apenas locais, fora do repo público
PLANO-MESTRE.md
CLAUDE.md
prompts/
relatorios/
docs/testes/
